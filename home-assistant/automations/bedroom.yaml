- trigger:
    - platform: time
      at: "10:00:00"
  action:
    - service: switch.turn_off
      entity_id:
        - switch.bedroom_outlet_0
  id: turn_off_fan_in_morning
  alias: Turn off fan in the morning

- trigger:
    - platform: time
      at: "10:00:00"
  action:
    - service: cover.open_cover
      entity_id:
        - cover.front_blinds
  id: open_front_blinds_in_morning
  alias: Open the front_blinds in the morning

- trigger:
    - platform: time
      at: "10:00:01"
  action:
    - service: cover.open_cover
      entity_id:
        - cover.back_blinds
  id: open_back_blinds_in_morning
  alias: Open the back_blinds in the morning

- trigger:
    - platform: time
      at: "10:00:02"
  action:
    - service: cover.open_cover
      entity_id:
        - cover.jon_blinds
  id: open_jon_blinds_in_morning
  alias: Open the jon_blinds in the morning

- trigger:
    - platform: time
      at: "10:00:03"
  action:
    - service: cover.open_cover
      entity_id:
        - cover.sarah_blinds
  id: open_sarah_blinds_in_morning
  alias: Open the sarah_blinds in the morning

- trigger:
    - platform: sun
      event: sunset
  action:
    - service: cover.close_cover
      entity_id:
        - cover.front_blinds
  id: close_front_blinds_in_evening
  alias: Close the front_blinds in the evening

- trigger:
    - platform: sun
      event: sunset
  action:
    - service: cover.close_cover
      entity_id:
        - cover.back_blinds
  id: close_back_blinds_in_evening
  alias: Close the back_blinds in the evening

- trigger:
    - platform: sun
      event: sunset
  action:
    - service: cover.close_cover
      entity_id:
        - cover.jon_blinds
  id: close_jon_blinds_in_evening
  alias: Close the jon_blinds in the evening

- trigger:
    - platform: sun
      event: sunset
  action:
    - service: cover.close_cover
      entity_id:
        - cover.sarah_blinds
  id: close_sarah_blinds_in_evening
  alias: Close the sarah_blinds in the evening
